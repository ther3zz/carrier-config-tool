# =================================================================
#  Docker Compose Deployment Profiles
# =================================================================
# A comma-separated list of service profiles to activate.
# - To run ONLY the FastAPI service, set this to: api
# - To run ONLY the Flask UI, set this to: ui
# - To run BOTH for development, set this to: api,ui
COMPOSE_PROFILES=api,ui

# =================================================================
#  Proxy Configuration
# =================================================================
# A comma-separated list of IP addresses for trusted reverse proxies.
# Set to "*" to trust all upstream proxies (common in container environments).
# This allows the app to correctly identify the client IP from X-Forwarded-For headers.
TRUSTED_PROXY_IPS=*

# Set the storage mode for credentials. Options: 'file' or 'db'.
CREDENTIAL_STORAGE_MODE=db

# The Friendly Name of the credential entry that corresponds to your
# main Vonage account. This account will be used to create new subaccounts
# when the `create_subaccount_if_not_found` flag is used.
VONAGE_PRIMARY_ACCOUNT_NAME=

# Defines the path on the host machine where log files will be stored.
LOGS_DIRECTORY=./logs



# The primary key used to encrypt and decrypt all credential secrets.
# IMPORTANT: Use a strong, random key for production.
MASTER_KEY=

# The API key external systems must provide to access the provisioning endpoint.
# IMPORTANT: Use a strong, random, and secret key.
FASTAPI_PROVISIONING_KEY=

# A comma-separated list of IP addresses allowed to access the API.
FASTAPI_IP_WHITELIST=

# --- MariaDB Database Connection Details ---
# IMPORTANT: Update DB_HOST to point to your existing database server.
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
