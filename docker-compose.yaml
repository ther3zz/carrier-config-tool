# --- START OF FILE docker-compose.yaml ---

version: '3.8'

services:
  # --- Flask Web Application (UI) ---
  flask_app:
    build: .
    container_name: flask_ui_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ${LOGS_DIRECTORY}:/app/logs
    env_file:
      - .env
    # --- START: MODIFICATION ---
    # Convert command to a single-line string for better compatibility.
    command: "flask run --host=0.0.0.0 --port=5000"
    # --- END: MODIFICATION ---
    restart: unless-stopped
    profiles:
      - ui

  # --- FastAPI Provisioning Service ---
  fastapi_app:
    build: .
    container_name: fastapi_provisioning_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ${LOGS_DIRECTORY}:/app/logs
    env_file:
      - .env
    # --- START: MODIFICATION ---
    command: "uvicorn fastapi_app:app --host 0.0.0.0 --port 8000 --reload --reload-exclude /app/logs/ --reload-exclude /app/config/"
    # --- END: MODIFICATION ---
    restart: unless-stopped
    profiles:
      - api

# --- END OF FILE docker-compose.yaml ---
